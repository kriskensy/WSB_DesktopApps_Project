<UserControl x:Class="MVVMFirma.Views.Reports.SACreportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views.Reports"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="white"  Width="600" HorizontalAlignment="left">
        <Grid.RowDefinitions>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="33"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Total time of all dives in the selected period for the selected user" Margin="3" HorizontalContentAlignment="Center"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Date from:" Margin="3"/>
            <DatePicker Grid.Column="1" Margin="3" SelectedDate="{Binding DateFrom}"/>
            <Label Grid.Column="2" Content="Date to:" Margin="3"/>
            <DatePicker Grid.Column="3" Margin="3" SelectedDate="{Binding DateTo}"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Select user" Margin="3"/>
            <ComboBox Grid.Column="1" Margin="3" 
      ItemsSource="{Binding UsersItems}" 
      DisplayMemberPath="Value" 
      SelectedValuePath="Key"
      SelectedValue="{Binding Path=IdUser, Mode=TwoWay}"/>
        </Grid>

        <Button Grid.Row="3" Content="Calculate" Margin="3" Command="{Binding CreateReport}"/>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Total dive time:" Margin="3"/>
            <Label Grid.Column="1" Content="{Binding DiveDurationTime}" Margin="3"/>
        </Grid>
    </Grid>
</UserControl>
