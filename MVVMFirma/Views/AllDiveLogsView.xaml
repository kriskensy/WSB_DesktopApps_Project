<local:AllViewBase x:Class="MVVMFirma.Views.AllDiveLogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="white">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <!--<DataGridTextColumn Header="Id" Binding="{Binding IdTowaru}"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding Kod}"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding Nazwa}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding StawkaVatZakupu}"/>
                <DataGridTextColumn Header="Phone Number" Binding="{Binding StawkaVatSprzedazy}"/>
                <DataGridTextColumn Header="Password Hash" Binding="{Binding Cena}"/>-->
            </DataGrid.Columns>
        </DataGrid>
            
    </Grid>
</local:AllViewBase>
